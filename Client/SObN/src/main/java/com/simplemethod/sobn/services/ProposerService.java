package com.simplemethod.sobn.services;

import org.springframework.stereotype.Service;

@Service
public class ProposerService {

    //Todo: Obsługa wysyłania zapytań do acceptora. prepare(seq), accept(seq,val), broadcast(seq), wysłania flagi z awarią i jej zdjęcia oraz pobierania informacji o aktualnym stanie acceptora.

}
